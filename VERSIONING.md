# 版本号规则

本项目使用三段式版本号：`主版本.次版本.修订版本`。

Git 标签在版本号前加 `v`，例如应用内版本为 `1.2.1` 时，对应 GitHub Release 标签为 `v1.2.1`。

## 三段版本号的含义

### 主版本

主版本是第一位数字，例如 `1.2.1` 中的 `1`。

只有出现以下任一情况时才增加主版本，增加后次版本和修订版本归零：

- 应用整体使用方式、页面结构或视觉体系发生明显重做。
- 完成大范围架构迁移，并且旧配置、旧数据或旧设备不再兼容。
- 最低支持的 HarmonyOS NEXT 版本提高，导致原本支持的设备不能继续使用。
- 现有重要功能被移除或改成不兼容的新行为。

示例：`v1.2.1` → `v2.0.0`。

### 次版本

次版本是第二位数字，例如 `1.2.1` 中的 `2`。

新增用户可以直接使用、并且足以单独说明的完整功能时增加次版本，增加后修订版本归零。已有功能的小调整不算新增完整功能。

适合增加次版本的情况：

- 新增发主题帖、草稿箱、私信、桌面卡片等完整能力。
- 新增一个可独立使用的主要页面或业务流程。
- 为已有能力补齐一个对用户有明显价值的大模块。

示例：`v1.2.1` → `v1.3.0`。

### 修订版本

修订版本是第三位数字，例如 `1.2.1` 中的 `1`。

修复问题、安全修复、性能优化、交互调整、样式调整，以及不构成完整新功能的小入口，都只增加修订版本。

适合增加修订版本的情况：

- 修复登录、退出登录、帖子操作、图片查看等已有功能的问题。
- 调整现有页面的布局、动画、点击区域或沉浸效果。
- 新增 QQ 群、Telegram 群等外部链接入口。
- 修复安全存储、网络请求或性能问题。

示例：`v1.2.0` → `v1.2.1`。

## 预发布版本

预发布版本用于测试即将发布的目标版本，格式为：

```text
v主版本.次版本.修订版本-beta.序号
v主版本.次版本.修订版本-rc.序号
```

- `beta`：功能已基本完成，仍需要日常测试。
- `rc`：发布候选版本，只允许修复阻断发布的问题。
- 序号从 `1` 开始递增；同一目标版本的 beta 和 rc 分别编号。
- 正式版发布后，不得继续为该正式版版本号创建新的 beta 或 rc。

示例：

```text
v1.2.2-beta.1
v1.2.2-beta.2
v1.2.2-rc.1
v1.2.2
```

如果下一轮是新功能开发，则从目标功能版开始：

```text
v1.3.0-beta.1
v1.3.0
```

## 版本字段对应关系

| 位置 | 正式版示例 | 测试版示例 |
|---|---|---|
| `AppScope/app.json5` 的 `versionName` | `1.2.1` | `1.2.2-beta.1` |
| Git 标签 | `v1.2.1` | `v1.2.2-beta.1` |
| GitHub Release 标题 | `TiebaLite HarmonyOS v1.2.1` | `TiebaLite HarmonyOS v1.2.2 Beta 1` |

`versionCode` 必须为整数，并随每次公开发布递增。为保持与现有 `1.2.0` 对应 `1200000` 的编码兼容，采用 `主版本 × 1,000,000 + 次版本 × 100,000 + 修订版本 × 100 + 构建序号` 的编码方式：

```text
1.2.1 的第 1 个构建：1200101
1.3.0 的第 1 个构建：1300001
2.0.0 的第 1 个构建：2000001
```

同一应用内版本重新构建时，只增加最后两位的构建序号；新的三段式版本号从构建序号 `01` 开始。

## 发布前检查

- 确认版本升级类型符合本规则。
- 同步更新 `versionName`、`versionCode`、Git 标签和 Release 标题。
- 在 `CHANGELOG.md` 写入从上一个正式版开始的完整变更。
- 按 `RELEASE_NOTES_GUIDE.md` 编写 GitHub Release 更新日志。
- 构建并验证正式 HAP 后再发布。
